# Minimal Example for segmentation fault on ntopng on arm

- this example creates a Fifo pipe filled with test pcap data ([origin](https://www.netresec.com/?page=PcapFiles))
- ntopng is set up to read from this pipe
